# PDF to Office conversion libraries
# Using PyMuPDF as primary for better compatibility
PyMuPDF==1.24.0
# pdf2docx for better formatting (may have bugs with some PDFs)
pdf2docx==0.5.8
tabula-py==2.9.0
openpyxl==3.1.2
python-pptx==0.6.23
pdf2image==1.17.0
Pillow==10.3.0
# For Word document creation in fallback
python-docx==1.1.0
